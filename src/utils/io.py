import cv2
import os
import numpy as np


def load_image(path):



def save_image(image, path):

def save_svg(paths_list, output_file, width, height):
    """Lưu danh sách các đường cong Bezier thành file SVG."""
